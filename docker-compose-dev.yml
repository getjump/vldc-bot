services:
  bot:
    build:
      context: .
      dockerfile: ./compose/dev/Dockerfile
    ports:
      - "5678:5678"
    volumes:
      - .:/app
      - .:/opt/gcloud
    env_file:
      - ./.env
    environment:
      - PYTHONPATH=./bot
    logging:
      options:
        max-size: "1M"
        max-file: "10"
